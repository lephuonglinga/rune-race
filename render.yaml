services:
  - type: web
    name: rune-race-server
    runtime: node
    buildCommand: "cd apps/server && pnpm build"
    startCommand: "node dist/index.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
